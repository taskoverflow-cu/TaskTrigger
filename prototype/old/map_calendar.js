$(function () {

    // map initialization
    (function (window) {     
    }(window));

    // buttons in header
    (function(window){

        // back to calendar
        $(".header .buttons .back").click(function(){
            window.location.href = 'calendar.html';
        });
    }(window));
})
